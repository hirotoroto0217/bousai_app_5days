# 指示・発信ボード移植ガイド

## AIへの統合依頼プロンプト

以下のプロンプトを、移植先の防災システムを開いている AI コーディングエージェントへ一度だけ入力してください。

```text
既存の防災システムを確認したうえで、今回作成した「指示・発信ボード」機能だけを、既存システムへ自然に統合してください。

## 基本方針

- 既存コードを先に確認すること。
- 同じ機能が存在する場合は重複実装せず、既存機能を利用すること。
- 既存のホーム画面、避難所検索、避難所登録などはできる限り変更しないこと。
- 特に避難所登録機能は既存実装を維持すること。
- 既存のデザイン、base.html の構成、認証方式に合わせること。
- 関係のないファイルや機能を変更しないこと。
- 実装後に動作確認を行うこと。
- 最後に変更内容と移植手順を Markdown ファイルで出力すること。

## デザイン・UI要件

- 既存システムの `base.html`、ヘッダー、ナビゲーション、フォント、ボタン、フォーム、余白、角丸、影のデザインを最優先で再利用すること。
- 既存デザインと異なる新しいテーマ、過度な装飾、不要なアニメーション、広告サイトのような大きなヒーロー領域は追加しないこと。
- 防災情報を素早く読み取れる、落ち着いた業務・公共サービス向けの画面にすること。
- 色だけに頼らず、ラベルやアイコン、枠線、文字の太さでも状態を判別できるようにすること。
- 十分なコントラストを確保し、リンク・ボタン・フォームはキーボードでも操作できるようにすること。
- 入力値や市民から届いた文章は HTML エスケープし、長い文字列や改行でレイアウトを壊さないこと。

### 全体レイアウト

- ページ上部にページ名と現在の利用者種別を表示すること。
- 管理者版では「発信情報」「指示一覧」「市民からの情報」の切り替えをまとめて配置すること。
- 「情報送信」は一覧切り替えとは異なる主要操作として、十分な余白を空けて配置すること。
- 「情報送信」は既存の青系タブと区別できる専用色にし、通常、ホバー、フォーカス、無効状態を定義すること。
- 一覧、中央の詳細、右側の絞り込み・操作の3領域を基本構成とすること。
- 中央の詳細欄には、タイトル、日時、種別、場所、緊急度、内容、操作ボタンを表示すること。
- 市民情報を選択した場合も一覧の選択状態を維持し、詳細欄で内容と対応操作を確認できるようにすること。
- 情報がない場合、読み込み中、通信エラーの場合の表示を用意すること。

### 一般利用者版

- ページ名は「掲示板」とすること。
- 行政からの発信情報を主役にし、タイトル、発信日時、災害種別、場所、緊急度を比較できるようにすること。
- 緊急度は「高」「中」「低」の文字ラベルを必ず表示すること。
- 発信情報の詳細は一覧選択後に中央欄または既存デザインに合う詳細ダイアログで表示すること。
- 「行政へ情報を送る」は通常の一覧操作と区別しやすい場所に配置すること。
- 送信フォームには項目ごとのラベル、必須・任意、入力上限を表示すること。

### 管理者版

- ページ名は「指示・発信ボード」とすること。
- 管理者向けタブまたはセグメントは選択中の状態を明確にすること。
- 「市民からの情報」の未読件数は数字バッジで表示し、0件の場合は表示しないこと。
- 未読情報は既読情報と区別し、「未読」ラベルを表示すること。
- 市民情報の詳細には、確認済み、対応中、対応完了などの操作を配置すること。
- 発信停止、解除など影響の大きい操作は警告色を使い、必要に応じて確認を求めること。
- 指示登録と公開情報発信では、送信種別に応じて不要な項目を隠すこと。

### 確認画面

- 送信前確認画面はモーダルまたは既存画面に合う確認ステップで表示すること。
- 見出しで、公開情報発信、職員向け指示、市民情報送信のどれかを明示すること。
- 送信される値を読み取り専用で一覧表示すること。
- 内容の改行を保持し、長い文章は折り返すこと。
- 「修正する」と「この内容で送信」を明確に分けること。
- 確認画面を開いただけでは保存・発信・状態変更を行わず、確定後だけ API を呼び出すこと。
- 送信中は確定ボタンを無効化し、二重送信を防ぐこと。
- モーダルを閉じても確定前の入力内容を失わないこと。

### レスポンシブ対応

- デスクトップでは一覧・詳細・絞り込みの3領域を配置すること。
- タブレットやスマートフォンでは縦方向に並べ、横スクロールを発生させないこと。
- ボタン、チェックボックス、セレクト、入力欄はタッチ操作しやすい大きさにすること。
- 長いタイトル、場所、内容、エラーメッセージを折り返すこと。
- モーダルは画面内に収まる最大高さとスクロールを設定すること。

## 画面表示と権限

- 指示・発信ボードの URL は `/board` とすること。
- ナビゲーションにはログイン状態に関係なく常に表示すること。
- 未ログイン時の名前、ページタイトル、見出しは「掲示板」とすること。
- 未ログイン時の `/board` は一般利用者版を表示すること。
- ログイン済みの場合は「指示・発信ボード」とし、管理者版を表示すること。
- 既存の `/citizen_board` が存在する場合は `/board` へリダイレクトして互換性を維持すること。

## 一般利用者版「掲示板」

- 発信中の行政情報を一覧表示し、選択すると詳細を表示すること。
- 災害種別、緊急度、タイトル、内容、場所などで絞り込めること。
- 発信停止された情報は表示しないこと。
- 行政へ災害情報を送信できること。
- 送信項目はタイトル、災害種別、場所、名前、内容とすること。
- 名前は任意入力とし、未入力は匿名として扱うこと。
- 送信直後に API を呼ばず、確認画面を表示すること。
- 「修正する」で入力画面へ戻り、「この内容で送信」で確定すること。

## 管理者版「指示・発信ボード」

次の4つを切り替えられるようにしてください。

1. 発信情報
2. 情報送信
3. 指示一覧
4. 市民からの情報

### 情報送信

- 住民・観光客向けの情報発信と職員向け指示登録を行えること。
- どちらも送信前に確認画面を表示すること。
- 情報送信ボタンは他のタブから離して配置し、専用色にすること。

### 発信情報

- 住民・観光客向けの情報を作成できること。
- タイトル、災害種別、場所、緊急度、内容を入力できること。
- 発信中の情報を一覧表示できること。
- 管理者が発信情報を停止できること。

### 指示一覧

指示には次を持たせてください。

- タイトル
- 対象部署または対象者
- 災害種別
- 場所
- 指示内容
- 対応状況
- 登録日時

対応状況は、指示中、対応中、完了、解除とし、キーワード、対象部署、対応状況で絞り込めるようにしてください。

### 市民からの情報

- 市民情報を一覧表示し、選択すると詳細欄に表示すること。
- 詳細を開いた時点で自動的に既読へ変更すること。
- 未読件数を表示すること。
- キーワード、未読／既読、災害種別、対応状況で絞り込めること。
- 対応状況は、報告済み、確認済み、対応中、対応完了、掲示済みとすること。
- 管理者が市民情報の状態を変更できること。
- 市民情報を引き継いで住民・観光客向け発信情報を作成できること。
- 市民情報を元に発信する場合も、発信前に確認画面を表示すること。

## JSON保存とAPI

データベースは使用せず、以下の JSON ファイルへ保存してください。

- `instructions.json`: 職員・部署向け指示
- `citizen_reports.json`: 市民から届いた情報
- `published_info.json`: 発信情報

必要な API は次のとおりです。

- `GET /api/published_info`
- `POST /api/add_citizen_report`
- `GET /api/instructions`
- `POST /api/add_instruction`
- `POST /api/update_instruction_status`
- `GET /api/citizen_reports`
- `POST /api/mark_citizen_report_read`
- `POST /api/update_report_status`
- `POST /api/publish_info`
- `POST /api/unpublish_info`

公開情報取得と市民情報登録は未ログインでも可能にし、指示操作、市民情報管理、情報発信・停止は管理者だけに制限してください。

## 実装後の確認

- 未ログイン時に「掲示板」と表示され、一般利用者版が開くこと。
- 管理者ログイン時に「指示・発信ボード」と表示され、管理者版が開くこと。
- 一般利用者の絞り込み、確認画面、送信が動作すること。
- 管理者の4機能切り替え、情報発信、指示登録、市民情報管理が動作すること。
- 情報送信ボタンが離れて配置され、専用色で表示されること。
- 市民情報の詳細表示時に既読化され、未読件数が更新されること。
- 市民情報から公開情報を作成できること。
- 発信情報を停止できること。
- ホーム画面、避難所検索、避難所登録が壊れていないこと。
- Python 構文、JSON 形式、可能な範囲の自動テストを実行すること。

調査だけで終了せず、必要なコード変更、テスト、Markdown による変更内容・実装手順の整理まで完了してください。
```

## 概要

指示・発信ボードは `/board` に集約します。ログイン状態をサーバー側のセッションで判定し、未ログイン時は一般利用者版、ログイン済みの場合は管理者版を同じ画面で表示します。既存のホーム画面、避難所検索、避難所登録の処理は変更しません。

## 今回の移植内容

- ナビゲーションに `/board` を常時表示
- 一般利用者版で発信中の行政情報を一覧・詳細表示
- 一般利用者版でキーワード、災害種別、緊急度による絞り込み
- 一般利用者から市民情報を行政へ送信
- 一般利用者・管理者ともに送信前の確認画面を表示
- 管理者版で4機能を切り替え
- 管理者による住民・観光客向け情報の発信と停止
- 管理者による部署向け指示の登録と対応状況更新
- 市民情報のキーワード、未読／既読、災害種別、対応状況による絞り込み
- 市民情報の詳細を開いた時点で既読化
- 未読件数の表示、状態更新
- 市民情報を元にした公開情報の作成
- 指示、市民情報、公開情報の JSON 保存
- 旧 `/citizen_board` から `/board` への互換リダイレクト

## 必要なファイル

| ファイル | 役割 |
|---|---|
| `app.py` | JSON 読込・保存、`/board`、認証保護された管理者 API、公開 API |
| `bousai_app/templates/base.html` | `/board` の常時ナビゲーション表示 |
| `bousai_app/templates/board.html` | 一般利用者版・管理者版の画面、絞り込み、送信、状態更新 |
| `bousai_app/templates/citizen_board.html` | 旧 URL 互換用。既存リンクがある場合に保持 |
| `bousai_app/data/instructions.json` | 職員・部署向け指示の保存先 |
| `bousai_app/data/citizen_reports.json` | 市民情報の保存先 |
| `bousai_app/data/published_info.json` | 発信情報の保存先 |

`shelters.json`、避難所関連テンプレート、ホーム画面固有の処理は移植対象ではありません。

## API一覧

| メソッド | パス | 権限 | 用途 |
|---|---|---|---|
| `GET` | `/api/published_info` | 全員 | 発信中の公開情報を取得 |
| `POST` | `/api/add_citizen_report` | 全員 | 市民情報を登録 |
| `GET` | `/api/instructions` | 管理者 | 指示一覧を取得 |
| `POST` | `/api/add_instruction` | 管理者 | 指示を登録 |
| `POST` | `/api/update_instruction_status` | 管理者 | 指示状態を更新 |
| `GET` | `/api/citizen_reports` | 管理者 | 市民情報一覧を取得 |
| `POST` | `/api/mark_citizen_report_read` | 管理者 | 市民情報を既読化 |
| `POST` | `/api/update_report_status` | 管理者 | 市民情報の対応状況を更新 |
| `POST` | `/api/publish_info` | 管理者 | 発信情報を発信。`report_id` で市民情報を元に作成可能 |
| `POST` | `/api/unpublish_info` | 管理者 | 発信情報を停止 |

移植先の認証実装が異なる場合は、`login_required` と `session.get('logged_in')` の部分だけを移植先の認証ヘルパーに置き換えます。公開情報取得と市民情報登録には管理者認証を付けないでください。

## 実装手順

1. `app.py` の JSON パス、`load_json`、保存関数、指示・市民情報・公開情報 API を移植する。
2. `base.html` の既存ナビゲーションに、認証条件の外側で `/board` のリンクを追加する。
3. `board.html` をテンプレートディレクトリへ配置し、テンプレートの `is_admin` に移植先セッションの認証状態を渡す。
4. 既存の `/citizen_board` が利用されている場合は、`/board` へのリダイレクトを残す。
5. 3つの JSON ファイルをデータディレクトリへ配置する。
6. `shelters.json` と避難所登録ルート・テンプレートに変更がないことを確認する。
7. 未ログインで `/board` を開き、公開情報の絞り込みと市民情報送信を確認する。
8. 管理者でログインし、4つの表示切替、未読件数、詳細表示時の既読化、状態更新、発信・停止を確認する。

## 移植時の注意

- 既存の避難所登録機能、ホーム画面、避難所検索機能を変更しないこと。
- JSON ファイルは UTF-8 で読み書きし、日本語を保持すること。
- 画面に出力するユーザー入力はテンプレートエンジンの自動エスケープを利用すること。
- API の権限を混同せず、公開 API と管理者 API を分けること。
- 保存・通信エラー時も画面を白紙にせず、利用者へ状態を表示すること。
- 既存の認証方式、レイアウト、命名規則を優先すること。
