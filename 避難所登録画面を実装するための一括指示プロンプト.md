# 避難所登録画面を実装するための一括指示プロンプト

以下の指示を、対象システムの AI コーディングエージェントに一度だけ入力してください。対象システムの既存構成、命名規則、認証方式、テンプレートエンジンに合わせて実装し、既存の別機能は壊さないでください。

---

あなたは既存の防災システムに、管理者向けの「避難所登録」機能を追加してください。実装前にリポジトリを確認し、既存のフレームワーク・ルーティング・テンプレート・データ保存方法を優先して使ってください。新しいフレームワークへの置き換えや無関係なリファクタリングは行わないでください。

## 目的

管理者が避難所を登録・確認・削除できる画面を作成する。登録データは既存システムの避難所データとして保存し、登録後も画面上の一覧に反映する。

## 実装対象

### 1. 管理者認証

- 避難所登録画面は未ログインでは表示しない。
- 未ログインでアクセスされた場合は、既存のログイン画面へリダイレクトする。
- ログイン成功後は、元々要求された避難所登録画面へ戻す。
- 既存の認証機構がある場合はそれを使い、新しい認証方式を作らない。
- 既存のログイン機構がない場合は、対象システムの方式に合わせて最小限の管理者認証を実装する。

### 2. ルート

既存の命名規則に合わせて、次の機能を持つ登録画面を追加する。

- `GET`: 登録フォームと登録済み避難所一覧を表示する。
- `POST` の通常登録: 入力値を検証し、問題がなければ新規登録して保存する。
- `POST` の削除: フォームの避難所名を対象として削除確認を表示する。
- 削除確認で「はい」を選んだ場合は対象データを削除して保存する。
- 削除確認で「いいえ」を選んだ場合は削除せず、通常の登録フォームへ戻す。
- 登録・削除後は成功メッセージを表示する。
- 保存に失敗した場合はエラーメッセージを表示し、入力値を失わない。

登録済み避難所の一覧では、避難所名だけを表示し、避難所名をクリックすると既存の避難所詳細画面へ遷移できるようにする。表示順は避難所名の昇順（日本語のあいうえお順を意図した文字列ソート）とする。登録済みデータがない場合は「登録されている避難所はありません。」と表示する。

### 3. データ項目

フォームには次の6項目を表示する。すべて必須とする。

| 項目 | フィールド名の例 | 入力形式 |
|---|---|---|
| 避難所名 | `name` | 文字列 |
| 住所 | `address` | 番地まで含む住所 |
| 収容人数 | `capacity` | 1以上の整数 |
| 電話番号 | `phone` | 一般的な電話番号 |
| 開設状況 | `status` | `開設` または `未開設` |
| ペット受け入れ | `pet_acceptance` | `可` または `不可` |

データには既存方式に合わせた一意な整数 ID も付与する。既存データの最大 ID に1を加え、データが空の場合は1から開始する。

データの保存先は、対象システムの既存の永続化方式を使う。JSON ファイルを使っている場合は、UTF-8・日本語を保持する形式で読み書きし、整形して保存する。アプリ起動時にデータを読み込み、登録・削除のたびに保存する。

### 4. 入力検証

サーバー側で必ず検証する。ブラウザの `required` 属性だけに依存しない。

- すべての入力値は前後の空白を除去する。
- 避難所名が空の場合は登録しない。
- 住所は空でないだけでなく、番地・番・号、またはハイフン区切りの番地まで含むことを要求する。
  - 例: `青森県青森市青葉3丁目1番2号`
  - 例: `東京都○○区○○町3-1-2`
- 住所欄に電話番号だけが入力された場合は住所として受け付けず、「住所：電話番号ではなく、番地まで入力してください。」と表示する。
- 番地までない住所には「住所：番地まで入力してください。」と表示する。
- 収容人数は1以上の整数だけを許可し、不正な場合は「収容人数：1以上の整数を入力してください。」と表示する。
- 電話番号は数字、空白、括弧、ハイフン（通常のハイフンと日本語入力由来のハイフンを含む）で構成された一般的な番号だけを許可し、不正な場合は「電話番号：正しい電話番号を入力してください。」と表示する。
- 開設状況が `開設` または `未開設` 以外の場合は、「開設状況：「開設」または「未開設」を選択してください。」と表示する。
- ペット受け入れが `可` または `不可` 以外の場合は、「ペット受け入れ：「可」または「不可」を選択してください。」と表示する。
- 必須項目または形式に不備がある場合は保存せず、「必須項目に不備があります。」を共通メッセージとして表示する。
- 同じ避難所名（前後空白を除去した値）が既に登録されている場合は保存せず、「過去に同じ避難所が登録されています。」と表示する。
- 検証エラー時は、入力済みの値をフォームに再表示する。

### 5. 画面

既存の共通レイアウトを継承し、他ページと視覚的に整合させる。登録画面には次を含める。

- 見出し「避難所登録」
- 説明文「新しい避難所情報を登録します。」
- 成功メッセージ領域
- エラーメッセージ領域と項目別エラー一覧
- 避難所名、住所、収容人数、電話番号のテキスト入力欄
- 開設状況のセレクトボックス
  - 未選択の案内
  - `開設`
  - `未開設`
- ペット受け入れのセレクトボックス
  - 未選択の案内
  - `可`
  - `不可`
- 「登録/更新」ボタン
- 「削除」ボタン
- 登録済み避難所一覧

フォームの入力欄には、次の例をプレースホルダーとして設定する。

- 避難所名: `例：○○小学校`
- 住所: `例：青森県青森市青葉3丁目1番2号`
- 収容人数: `例：500`
- 電話番号: `例：017-0000-0000`

削除ボタンは通常の必須入力検証を無効にして送信できるようにする。ただし削除対象が特定できるよう、避難所名を使う。削除時はいきなり削除せず、同じ画面内で「データを削除しますか？」と確認し、「はい」「いいえ」の2つのボタンを表示する。「いいえ」ではデータを変更しない。

成功時は以下のメッセージを使う。

- 登録: `登録完了しました！`
- 削除: `データを削除しました。`

失敗時は、原因に応じたエラーメッセージを表示し、ユーザーが入力を修正できる状態にする。

### 6. セキュリティと品質

- テンプレートに表示する値は、使用しているテンプレートエンジンの自動エスケープを有効にする。
- 削除対象は避難所名だけでなく、現在の保存データと照合して処理する。
- 外部 URL へ無制限にリダイレクトしない。ログイン後の戻り先を受け付ける場合は同一ホストか検証する。
- ファイル保存やデータ読み込みの例外を適切に処理し、画面を白紙にしない。
- 既存の API、検索画面、詳細画面、別ページの挙動を変更しない。
- 既存コードのスタイルと日本語表示を維持する。
- CSRF 対策など、既存システムに導入済みのセキュリティ機構があれば維持する。

## テスト要件

実装後、既存のテスト方式に合わせて登録機能のテストを追加または更新し、少なくとも次を確認する。

1. 未ログインでは登録画面へ直接アクセスできない。
2. 空の避難所名では登録されず、共通エラーが表示される。
3. 正しい6項目を送信すると登録され、登録完了メッセージが表示される。
4. 登録済み避難所一覧に避難所名が表示される。
5. 一覧の避難所名が名前順に並び、詳細画面へのリンクを持つ。
6. 住所に番地がない場合は項目別エラーが表示される。
7. 住所欄に電話番号を入力した場合は住所エラーが表示される。
8. 収容人数、電話番号、開設状況、ペット受け入れの不正値が、それぞれ項目別エラーになる。
9. 同じ避難所名を再登録できない。
10. 削除ボタンで確認画面が表示され、確認前にはデータが削除されない。
11. 削除確認で「いいえ」を選ぶとデータが残る。
12. 削除確認で「はい」を選ぶとデータが削除され、削除完了メッセージが表示される。
13. 保存に失敗した場合、エラー表示と入力値の再表示が行われる。

最後に、対象プロジェクトの通常のテストコマンドと、可能なら対象テストだけを実行する。失敗したテストは原因を修正して再実行し、変更したファイルと検証結果を簡潔に報告する。

## 実装完了条件

- 管理者だけが避難所登録画面を利用できる。
- 6項目の登録、入力保持付きエラー表示、重複防止、一覧表示、詳細リンク、削除確認、JSON または既存方式での永続化が動作する。
- 上記テスト要件を満たす。
- 既存の別機能に不要な変更がない。
