# 防災アプリ完成版を作るための一括指示プロンプト

あなたはこのワークスペースの既存 Flask アプリを完成させる実装担当 AI です。以下の要件をすべて読み、確認質問をせず、既存コードを調査したうえで実装・検証まで一度に行ってください。

## 基本方針

- 対象は既存の `/workspaces/bousai_app_5days` です。
- 既存の技術構成（Flask、Jinja テンプレート、JSON データ、Leaflet）を維持してください。
- ホーム画面はすでに作成済みです。**ホーム画面の大枠、配置、パネル構成、既存のデザインを変更しないでください。**
- ホーム画面にある以下の領域をそのまま活用してください。
  - マップ領域
  - 「災害の種類」「避難所」のチェックリスト
  - 「行政の指示・お知らせ」
  - 「避難所候補」
  - 気象情報パネル
- 不要な全面リニューアル、別ページへの機能移動、既存 API の破壊、無関係なリファクタリングは行わないでください。
- 編集後は、Python 構文、テンプレートのレンダリング、JavaScript 構文、主要 API の動作を確認してください。

## 1. お知らせ・指示ボードの連携

- 既存の指示ボードに登録された住民向けのお知らせを、ホーム画面の「行政の指示・お知らせ」に表示してください。
- 更新日時または作成日時の新しい順に並べ、ホーム画面には最新の最大3件を表示してください。
- お知らせがない場合は「お知らせはありません」と表示してください。
- 指示ボード側の既存機能とデータを壊さないでください。
- お知らせの本文は HTML として解釈せず、Jinja の自動エスケープを有効にしてください。

## 2. 避難所の詳細表示

避難所には最低限、次の情報を持たせてください。

- 避難所名
- 住所
- 開設状況：`未開設`、`開設中`、`閉鎖`
- 混雑状況：`不明`、`空き`、`やや混雑`、`混雑`、`満員`

避難所登録画面では、避難所名と住所を必須入力にし、開設状況と混雑状況を選択入力できるようにしてください。

ホーム画面の「避難所候補」には、次を表示してください。

- 避難所名
- 住所
- 現在地からの距離
- 開設状況
- 混雑状況
- 「ルートを検索」リンク

避難所候補は距離の近い順に最大3件を表示してください。住所が未登録、または座標取得に失敗した施設は、推測した場所に表示せず、位置情報未登録として扱ってください。

## 3. マップ内の避難所表示

- 登録された避難所の住所を使って位置情報を取得し、実際の住所に対応する場所へ避難所マーカーを表示してください。
- 固定した避難所名と固定座標の辞書だけに依存しないでください。
- 避難所マーカーをタップすると、避難所名、住所、開設状況、混雑状況を表示してください。
- 「避難所」チェックボックスで避難所レイヤーの表示・非表示を切り替えられるようにしてください。
- 住所から座標を取得する処理が失敗しても、ホーム画面全体が停止しないようにしてください。

## 4. 現在地から避難所までのルート検索

- ブラウザの位置情報 API で現在地を取得してください。
- 現在地が取得できない場合は、青森市の代表地点を代替地点として使用してください。
- 避難所候補ごとに、現在地から目的地までの徒歩ルートを検索できるリンクを表示してください。
- リンクは新しいタブで開き、外部 URL には `rel="noopener"` を付けてください。
- 既存のホーム画面の構成を崩さず、避難所候補の各項目内に導線を追加してください。

## 5. 別システムからの災害報告連携

別アプリから災害の種類と場所を送信できるよう、既存の Flask アプリに JSON API を用意してください。

エンドポイント：`POST /api/disaster_reports`

リクエストに必要な項目：

```json
{
  "type": "雨",
  "title": "道路冠水",
  "description": "道路が冠水しています",
  "address": "青森市中央1-1",
  "latitude": 40.8244,
  "longitude": 140.7400
}
```

要件：

- 必須項目が不足している場合は HTTP 400 を返してください。
- 正常登録時は HTTP 201 と登録データを返してください。
- `GET /api/disaster_reports` で登録済み報告を取得できるようにしてください。
- JSON ファイルなど、既存プロジェクトに合った方法で永続化してください。
- 災害報告の種類は、少なくとも `雨`、`大雨`、`洪水`、`土砂災害`、`地震`、`津波`、`火災` を扱えるようにしてください。
- 将来別アプリから接続できるよう、リクエスト形式を明確で単純な JSON にしてください。

ホーム画面では、災害報告を次のように扱ってください。

- 災害の種類に対応した絵文字または視認しやすいアイコンをマップ上に表示する。
  - 雨・大雨：傘または雨
  - 洪水・津波：水・波
  - 土砂災害：山
  - 地震：警告
  - 火災：火
- マーカーをタップすると、災害名、説明、住所、報告時刻を表示する。
- 「災害の種類」チェックボックスで災害報告レイヤーの表示・非表示を切り替える。

## 6. 青森市の警戒レベル相当情報

- 気象庁の青森県警報データから、青森市の市区町村コード `0220100` の情報を取得してください。
- 警報・注意報の全種類を取りこぼさず表示してください。
- 少なくとも以下を正しく表示してください。
  - レベル5相当の大雨特別警報
  - レベル4相当の大雨・高潮・土砂災害
  - レベル3相当の大雨・高潮・土砂災害
  - レベル2相当の大雨・高潮・土砂災害
  - 洪水、雷、強風、大雪、波浪などの警報・注意報
- 気象庁の状態が `発表`、`継続` だけでなく、`危険警報から警報`、`警報から注意報`、`危険警報から注意報` のような状態遷移の場合も、有効な情報として扱ってください。
- 状態遷移中の情報を画面に表示するときは、利用者に分かりやすく `発表` と表示して構いません。
- レベル3大雨警報のコード `03` を正しく「レベル3大雨警報」と表示してください。
- 気象庁 API が一時的に失敗しても、画面全体を壊さず、取得失敗を表示してください。

## 7. 天気情報

青森市の天気情報を取得して、既存の気象情報パネル内に表示してください。

- 現在の天気
- 降水確率
- 最高気温
- 最低気温
- 現在の気温

外部 API は、既存の依存関係を大きく増やさない方法を優先してください。API の取得に失敗した場合は、警報・注意報の表示まで巻き込んで失敗させず、「天気情報を取得できませんでした」と表示してください。

天気は、晴れ・曇り・雨・雪などをアイコンまたは絵文字で判別できるようにしてください。

## 8. 画面上の細かい制約

- ホーム画面の言語選択 UI は表示しないでください。
- ホーム画面のマップ枠内にあるタイトルは「防災アプリ」ではなく「ホーム」としてください。
- チェックリストは「災害の種類」と「避難所」の2項目だけにしてください。
- 既存の文字サイズ変更機能は維持してください。
- スマートフォン幅でも、情報やボタンが重ならないようにしてください。

## 9. 実装後の確認

実装後、次を実行して問題を修正してください。

1. `python -m py_compile app.py`
2. Flask のテストクライアントで `/` が HTTP 200 を返すこと
3. `POST /api/disaster_reports` の必須項目エラーが HTTP 400 になること
4. 正常な災害報告登録が HTTP 201 になること
5. 気象庁コード `03` が「レベル3大雨警報」に変換されること
6. 天気 API の4つの数値項目が取得・表示できること
7. Jinja 展開後の JavaScript が構文エラーにならないこと
8. `git diff --check`

最後に、変更したファイル、実装した機能、検証結果、外部 API が利用できない場合の制約を簡潔に報告してください。
